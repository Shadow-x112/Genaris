[project]
name = "genaris"
version = "0.1.0"
description = "Project Genaris - a living fantasy-world simulation (Slice 0: minimal agent survival loop)"
readme = "README.md"
requires-python = ">=3.11"
dependencies = []

[project.scripts]
genaris = "genaris.main:main"

[build-system]
requires = ["uv_build>=0.12.13,<0.13.0"]
build-backend = "uv_build"
